- name: CKA lab
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  roles:
    - role: lab
      vars:
        lab_task: "{{ task | default('') }}"
        lab_action: "{{ action | default('deploy') }}"
