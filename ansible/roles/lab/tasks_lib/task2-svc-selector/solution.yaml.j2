apiVersion: v1
kind: Service
metadata:
  name: web-svc
  namespace: {{ lab_namespace }}
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
